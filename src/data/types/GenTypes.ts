export type DataProps = {
  site: {
    siteMetadata: {
      title: string;
      description: string;
      keywords: string;
      author: string;
      siteUrl: string;
      featuredImage: any;
    };
  };
};
